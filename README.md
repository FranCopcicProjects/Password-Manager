# Password-Manager
I implemented a command-line password manager in Python with "init", "put", and "get" functions. This password manager uses AES-256-CBC encryption, PBKDF2 key derivation, and HMAC for integrity. It also includes a shell script for automated setup and testing of all functionalities.
